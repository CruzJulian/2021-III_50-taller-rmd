

```{r}
com <- "company_name"

tabla_datos_1 %>% 
  filter(company == com) %>% 
  ggplot + 
  aes(title) +
  geom_bar() +
  coord_flip() +
  theme_minimal() -> gg_obj_title_freq

```

## `r com`

A continuación, se muestra la distribución del recurso humano de acuerdo a su título. Es posible observar en la gráfica, tanto la variedad en profesionales de la compañía `r com` como la concentración/dispersión de los roles y profesiones en su personal.

```{r, fig.show="asis", fig.width=7, fig.height=5, include = TRUE}
gg_obj_title_freq

```




